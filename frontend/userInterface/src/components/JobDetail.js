import React from "react";
import { Link } from 'react-router-dom';



const Jobs = props => {
    const allJobs = Object.values(props.jobDetail)

    return (
        <div>
            <h1>Job Detail: </h1>
        </div>
    );
};


export default Jobs;